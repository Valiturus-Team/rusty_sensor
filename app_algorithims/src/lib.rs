

// we can copy stuff across from algo dev to get started 

THIS PROJECT IS NOT SETUP YET COPY lib/src config in app sub crate

pub fn add(left: usize, right: usize) -> usize {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
